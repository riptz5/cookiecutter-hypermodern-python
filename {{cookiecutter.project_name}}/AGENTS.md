
# AGENTS.md

![AGENTS.md logo](https://agents.md/og.png)

This file acts as the **single source of truth** for AI agents working on **{{cookiecutter.friendly_name}}**.

## Project Overview
{{cookiecutter.project_short_description}}

## Development Environment

### Tools
- **Dependency Management**: [Poetry](https://python-poetry.org/)
- **Task Runner**: [Nox](https://nox.thea.codes/)

### Quick Start
```bash
pip install nox poetry
poetry install
```

## Where Things Live

```
{{cookiecutter.project_name}}/
├── src/
│   └── {{cookiecutter.package_name}}/   # Main package code
│       ├── __init__.py
│       └── __main__.py
├── tests/                               # Test suite
├── docs/                                # Documentation (Sphinx)
├── noxfile.py                           # Nox automation sessions
├── pyproject.toml                       # Project metadata & dependencies
└── AGENTS.md                            # This file
```

### Future Structure (Reserved)
When extending this project, consider these directories:
- `agents/` - AI agent implementations
- `core/` - Core business logic
- `adapters/` - External service adapters/integrations

## Testing & Verification
We use `nox` for all testing and automation tasks.

### Reference Commands
| Command | Purpose |
|---------|---------|
| `nox` | Run full test suite and linting (Default) |
| `nox -s tests` | Run unit tests only |
| `nox -s lint` | Run code quality checks |
| `nox -s docs` | Build documentation |

### Code Quality Rules
- **Coverage**: 100% test coverage is required.
- **Style**: Code must be formatted with `black` and pass `flake8` linting.

## Recommended Workflow for AI Agents

1. **Explore**: Read this `AGENTS.md` and `pyproject.toml` to understand the project.
2. **Plan**: Draft changes in a `<thinking>` block before implementing.
3. **Implement**: Make focused, incremental changes.
4. **Test**: Run `nox` after every change to catch regressions.
5. **Verify**: Ensure 100% test coverage before completing.

### Agent Boundaries
- **⛔ DO NOT** push to `main` without PR review.
- **⛔ DO NOT** skip test verification.
- **✅ DO** run `nox -s tests` frequently during development.
- **✅ DO** consult `nox -l` for all available sessions.

## Contribution Guidelines
- **Tests**: Verify all changes with `nox`.
- **Docs**: Update documentation for any new features.

---
*Generated by [cookiecutter-hypermodern-python](https://github.com/cjolowicz/cookiecutter-hypermodern-python) following the [AGENTS.md](https://agents.md) standard.*
